name: Workflow 

on: [pull_request]

jobs:
  pull-request:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: START Workflow 
        run: |
          echo "START"
